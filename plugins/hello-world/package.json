{
	"name": "hello-world",
	"version": "1.0.0",
	"private": true,
	"description": "Example plugin demonstrating the monorepo shared library.",
	"author": "smiledragon-fe",
	"license": "MIT",
	"scripts": {
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"dev": "node esbuild.config.mjs",
		"clean": "rm -rf dist"
	},
	"devDependencies": {
		"@repo/config": "workspace:*",
		"@repo/shared": "workspace:*",
		"obsidian": "latest",
		"tslib": "^2.6.2",
		"typescript": "^5.4.0"
	}
}
